<h1>Links</h1>

<ul id="links">
	<% @links.each do |link| %>
		<li>
			Title: <%= link.title %>
			URL: <%= link.url %>
			Tags: <%= link.tags.each { |tag| print tag } %>  <!--no tags value -->
		</li>
	<% end %>
</ul>